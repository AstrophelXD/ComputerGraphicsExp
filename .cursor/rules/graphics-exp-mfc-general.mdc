---
description: General rules for GraphicsExp MFC experiments
alwaysApply: true
---

# GraphicsExp 通用规则（MFC）

## 技术栈与工程约束

- 默认使用 **MFC + OpenGL** 实现图形实验，不引入 GLUT/Qt 等替代框架。
- 若用户未明确要求，不新增 CMake、vcpkg、跨平台脚本；优先沿用 `build_mfc.bat`。
- 代码与脚本默认放在对应实验目录（如 `exp1/`, `exp2/`, `exp6/`）。

## 实现策略

- 严格按实验题目要求实现；若只要求某一组（如 21 组及以后），只实现该范围。
- 优先写“可交付的最小实现”，避免过度工程化和无关功能扩展。
- MFC 结构优先采用清晰单文件或最小类结构（`CWinApp/CFrameWnd/CView`）。
- 能删就删：未使用的分支、形体、材质、交互按键不保留。

## 代码风格

- 默认 ASCII；注释简短、只解释关键数学/图形逻辑。
- 统一命名、减少重复代码；禁止出现同文件重复拼接实现。
- 变更后同步更新实验目录 `README.md`（实现范围、运行方式、已知限制）。

## 验证要求

- 每次实质性修改后必须本地构建验证（优先执行 `build_mfc.bat`）。
- 若构建失败，先修复再结束，不把未验证状态当作完成。
- 输出中应明确说明“已验证通过/未通过及原因”。
